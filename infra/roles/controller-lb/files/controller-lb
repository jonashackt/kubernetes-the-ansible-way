http {
    upstream controller.k8s {
        server controller-0.k8s;
        server controller-0.k8s;
        server controller-0.k8s;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://controller.k8s;
        }
    }
}
